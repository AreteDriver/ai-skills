## Diagnosis: Authentication test regression after dependency upgrade

### Failure Signature
- **Test:** `test_user_login` in `tests/auth/test_login.py:42`
- **Error:** `AssertionError: assert 401 == 200`
- **Context:** Login endpoint returning 401 Unauthorized during test execution after pydantic v2 bump

### Taxonomy
- **Technical mode:** `contract_violation`
- **Content mode:** `F1_missing_constraint` (import assumption broken), `F2_wrong_assumption` (test assumes 200)
- **CI mode:** `dependency_failure`
- **Justification:** Dependabot upgraded `pydantic` to v2, which moved `BaseModel` import path. This broke auth models that relied on the old import, cascading into login logic.

### Diagnosis
The login test `test_user_login` is asserting a 200 status code but receiving 401. This indicates a regression in the authentication flow, likely introduced in commit `abc123` which modified the auth middleware to use the new pydantic v2 `BaseModel` import. The test expectation is now out of sync with the implementation due to the model validation path changing.

### Severity
High — localized to auth test, but the underlying import issue will affect production auth.

### Likely Culprit
Commit `abc123` — `build(deps): bump pydantic from 1.10 to 2.0`.

### Recommended Fix
Update the auth module to use the pydantic v1 compatibility import, or migrate the codebase fully to pydantic v2. Short-term patch:

```diff
--- a/src/auth/models.py
+++ b/src/auth/models.py
@@ -1,4 +1,4 @@
-from pydantic import BaseModel
+from pydantic.v1 import BaseModel
 ```

### Prevention
- Pin major dependency bumps in `dependabot.yml` to require human review.
- Add a smoke test that imports all model modules to catch import-level regressions.

---

*Generated by CI Diagnostician | Confidence: high*
