# Eve Frontier Api Response
## Example Output
```
WORLD_API_BASE = "https://blockchain-gateway-stillness.live.tech.evefrontier.com"
# Public endpoints — no auth header required
# Authenticated endpoints (e.g. /jumps) require Bearer token
```
